overwrite: true
schema: ../server/src/compiled/schema.gql
documents: ./src/**/*.graphql
generates:
  ./src/api/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
